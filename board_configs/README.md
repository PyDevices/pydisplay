# board_configs — moved

Board configs live in the sibling repo
**[PyDevices/micropython-hardware](https://github.com/PyDevices/micropython-hardware)**
(`board_configs/`).

```text
~/gh/pydevices/micropython-hardware/board_configs/
```

MIP:

```python
mip.install("github:PyDevices/micropython-hardware/board_configs/…")
```

pydisplay’s default desktop/browser config remains
[`src/lib/board_config.py`](../src/lib/board_config.py).
