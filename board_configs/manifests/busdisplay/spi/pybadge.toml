# Bootstrapped from board_configs — edit here; run generate_board_configs.py
kind = "busdisplay_verbatim"
slug = 'pybadge'
out = "busdisplay/spi"
circuitpython = true
title = 'Adafruit PyBadge LC ST7789 + shift-register buttons — MicroPython (SAMD51)'

[verbatim.mp]
board_config = '''
"""Adafruit PyBadge LC ST7789 + shift-register buttons — MicroPython (SAMD51)"""

from keypad_shift import PYBADGE_BUTTON_MAP, ShiftRegisterButtons
from spibus import SPIBus
from st7789 import ST7789

import eventsys

display_bus = SPIBus(
    id=0,
    baudrate=24_000_000,
    sck=45,
    mosi=47,
    miso=46,
    dc=37,
    cs=39,
    reset=0,
)

display_drv = ST7789(
    display_bus,
    width=320,
    height=240,
    colstart=0,
    rowstart=0,
    rotation=0,
    mirrored=False,
    color_depth=16,
    bgr=False,
    reverse_bytes_in_word=True,
)

buttons = ShiftRegisterButtons(
    clock=63,
    latch=32,
    data=62,
    mapping=PYBADGE_BUTTON_MAP,
)

runtime = eventsys.Runtime(display=display_drv)
runtime.add_keypad(read=buttons.read)
'''
package_json = """
{
  "urls": [
    ["board_config.py", "github:PyDevices/pydisplay/board_configs/busdisplay/spi/pybadge/board_config.py"],
    ["st7789.py", "github:PyDevices/pydisplay/drivers/display/st7789.py"],
    ["keypad_shift.py", "github:PyDevices/pydisplay/drivers/input/keypad_shift.py"]
  ],
  "deps": [
    ["github:PyDevices/pydisplay/packages/displaysys.json", "main"],
    ["github:PyDevices/pydisplay/packages/spibus.json", "main"]
  ],
  "version": "0.1"
}
"""

[verbatim.cp]
board_config = '''
"""Adafruit PyBadge LC ST7789 320x240 — CircuitPython"""

import board
from displayio import release_displays
from fourwire import FourWire
from keypad_gpio import GPIOButtons, MAGTAG_BUTTON_KEYS
from st7789 import ST7789

import eventsys

release_displays()

display_bus = FourWire(
    board.SPI(),
    command=board.TFT_DC,
    chip_select=board.TFT_CS,
    reset=board.TFT_RESET,
    baudrate=24_000_000,
)

display_drv = ST7789(
    display_bus,
    width=320,
    height=240,
    colstart=0,
    rowstart=0,
    rotation=0,
    mirrored=False,
    color_depth=16,
    bgr=False,
    reverse_bytes_in_word=True,
)

buttons = GPIOButtons(
    {
        "a": (board.BUTTON_A, MAGTAG_BUTTON_KEYS[0]),
        "b": (board.BUTTON_B, MAGTAG_BUTTON_KEYS[1]),
        "c": (board.BUTTON_C, MAGTAG_BUTTON_KEYS[2]),
        "d": (board.BUTTON_D, MAGTAG_BUTTON_KEYS[3]),
    }
)

runtime = eventsys.Runtime(display=display_drv)
runtime.add_keypad(read=buttons.read)
'''
package_json = """
{
  "urls": [
    ["board_config.py", "github:PyDevices/pydisplay/board_configs/busdisplay/spi/cp_pybadge/board_config.py"],
    ["st7789.py", "github:PyDevices/pydisplay/drivers/display/st7789.py"],
    ["keypad_gpio.py", "github:PyDevices/pydisplay/drivers/input/keypad_gpio.py"]
  ],
  "deps": [
    ["github:PyDevices/pydisplay/packages/displaysys.json", "main"]
  ],
  "version": "0.1"
}
"""
