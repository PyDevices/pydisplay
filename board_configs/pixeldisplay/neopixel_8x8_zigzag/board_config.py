"""NeoPixel 8x8 matrix — MicroPython (requires neopixel + grid mapper)"""

import eventsys

raise NotImplementedError(
    "Addressable LED grid on MicroPython requires neopixel cmod — "
    "use cp_neopixel_8x8_zigzag on CircuitPython"
)
