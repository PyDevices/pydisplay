# Dev / CI tools — install into repo-root .venv only:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/playwright install chromium

playwright>=1.40
pytest>=8.0
pytest-timeout>=2.2
