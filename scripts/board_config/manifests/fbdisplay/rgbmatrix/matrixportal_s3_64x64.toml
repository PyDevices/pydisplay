kind = "fbdisplay_rgbmatrix"
slug = "matrixportal_s3_64x64"
title = "MatrixPortal S3 64x64 HUB75 — MicroPython"
title_cp = "MatrixPortal S3 with 64x64 HUB75 RGB matrix — CircuitPython"
out = "fbdisplay"
circuitpython = true
cp_matrix_alias = true

[matrix]
width = 64
height = 64
bit_depth = 4
rgb_pins = [42, 41, 40, 38, 39, 37]
addr_pins = [45, 36, 48, 35, 21]
clock_pin = 2
latch_pin = 47
output_enable_pin = 14
doublebuffer = true

[matrix.cp]
width = 64
height = 64
bit_depth = 4
rgb_pins = ["board.MTX_R1", "board.MTX_G1", "board.MTX_B1", "board.MTX_R2", "board.MTX_G2", "board.MTX_B2"]
addr_pins = ["board.MTX_ADDRA", "board.MTX_ADDRB", "board.MTX_ADDRC", "board.MTX_ADDRD", "board.MTX_ADDRE"]
clock_pin = "board.MTX_CLK"
latch_pin = "board.MTX_LAT"
output_enable_pin = "board.MTX_OE"
doublebuffer = true

[fbdisplay]
width = 64
height = 64
